import 'package:flutter/material.dart';

class Hub_Screen extends StatelessWidget {
  const Hub_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Hub Screen'),);
  }
}