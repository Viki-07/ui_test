import 'package:flutter/material.dart';

class Learn_Screen extends StatelessWidget {
  const Learn_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Learn Screen'),);
  }
}